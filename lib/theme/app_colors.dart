import 'package:flutter/material.dart';

class AppColors {
  // Primary Colors
  static const Color primary = Color(0xFF0D2444);  // Main app color (dark blue)
  static const Color primaryDark = Color(0xFF081A33);  // Darker shade of primary
  static const Color primaryLight = Color(0xFF1A2F55);  // Lighter shade of primary

  // Background Colors
  static const Color background = Color(0xFFF5F5F5);  // Light grey from original theme
  static const Color surface = Colors.white;
  static const Color cardBackground = Colors.white;

  // Text Colors
  static const Color textPrimary = Color(0xFF212121);
  static const Color textSecondary = Color(0xFF757575);
  static const Color textLight = Colors.white;

  // Accent Colors
  static const Color accent = Color(0xFF4ECDC4);  // Teal accent color from original theme
  static const Color success = Color(0xFF4CAF50);
  static const Color error = Color(0xFFE53935);
  static const Color warning = Color(0xFFFFC107);

  // Border and Divider Colors
  static const Color border = Color(0xFFE0E0E0);
  static const Color divider = Color(0xFFEEEEEE);

  // Icon Colors
  static const Color iconPrimary = Color(0xFF212121);
  static const Color iconSecondary = Color(0xFF757575);
  static const Color iconLight = Colors.white;

  // Button Colors
  static const Color buttonPrimary = Color(0xFF0D2444);  // Same as primary
  static const Color buttonSecondary = Color(0xFF757575);
  static const Color buttonDisabled = Color(0xFFBDBDBD);
} 