import 'package:flutter/material.dart';

class AssignmentsTabWidget extends StatelessWidget {
  const AssignmentsTabWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Assignments feature coming soon'),
    );
  }
} 